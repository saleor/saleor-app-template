query FetchProducts($count: Int!, $channel: String!) {
    products(first: $count, channel: $channel) {
        edges {
            node {
                id,
                name
            }
        }
    }
}