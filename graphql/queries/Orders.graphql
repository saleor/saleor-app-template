query Orders {
  orders(first: 10) {
    edges {
      node {
        id
        number
        status
        created
        user {
          firstName
          lastName
        }
      }
    }
  }
}
